export interface ClientInquiry {
  name: string;
  email: string;
  phone?: string;
  company?: string;
  website?: string;
  projectType: string;
  budget: string;
  timeline?: string;
  details: string;
  source?: string;
}
